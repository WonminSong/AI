KNN k - nearest neighbors algorithm

처음으로는 k를 정한다.
k는 새로운 데이터를 분류할 때 새로운 데이터에서 가장 가까운 순으로 데이터들의 개수를 정하는 데 사용된다.
선정된 k개의 데이터는 같은 종류의 데이터의 개수가 더 많은 것을 기준으로 새로운 데이터의 종류를 판단한다.
때문에 2대2 와 같은 상황을 방지하기 위하여 k는 홀수로 정한다

느낀점 : knn으로 분류 지도가 만들어지는 것을 보고 이런식으로도 분류를 할 수 있구나라고 생각하였다.
개인적인 생각으로는 이 알고리즘이 좋은 성능을 가진다는 점이 놀라웠다.


참고:
https://scikit-learn.org/stable/modules/neighbors.html#nearest-neighbors-classification
